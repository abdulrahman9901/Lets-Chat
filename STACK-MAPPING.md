This repository uses Cursor stack rules under `stacks/`.

## Detected stacks

- **django** → `stacks/django.mdc`
- **svelte** → `stacks/svelte.mdc`
- **react (legacy)** → `stacks/react.mdc`
- **typescript** → `stacks/typescript.mdc`

If a new stack is introduced, add a `stacks/<stack>.mdc` file and list it here.

